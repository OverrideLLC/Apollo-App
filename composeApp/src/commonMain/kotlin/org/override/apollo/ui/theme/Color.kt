package org.override.apollo.ui.theme

import androidx.compose.ui.graphics.Color

// =================== TEMA CLARO ===================

// Colores Primarios - Verde azulado profesional y elegante
val lightPrimary = Color(0xFF00695C) // Verde azulado profundo profesional
val lightOnPrimary = Color(0xFFFFFFFF) // Blanco para contraste
val lightPrimaryContainer = Color(0xFFB2DFDB) // Verde azulado muy claro para contenedores
val lightOnPrimaryContainer = Color(0xFF002019) // Verde muy oscuro para texto

// Colores Secundarios - Verde grisáceo complementario
val lightSecondary = Color(0xFF4A5C56) // Verde grisáceo para elementos secundarios
val lightOnSecondary = Color(0xFFFFFFFF) // Blanco para contraste
val lightSecondaryContainer = Color(0xFFCCE8DF) // Verde grisáceo claro
val lightOnSecondaryContainer = Color(0xFF051B15) // Verde grisáceo muy oscuro

// Colores Terciarios - Marrón verdoso para elementos de acento
val lightTertiary = Color(0xFF5D6B42) // Verde oliva para elementos especiales
val lightOnTertiary = Color(0xFFFFFFFF) // Blanco para contraste
val lightTertiaryContainer = Color(0xFFE1EFBE) // Verde oliva muy claro
val lightOnTertiaryContainer = Color(0xFF1B1F04) // Verde oliva muy oscuro

// Colores de Error - Rojo profesional (mantener igual)
val lightError = Color(0xFFBA1A1A) // Rojo profesional
val lightOnError = Color(0xFFFFFFFF) // Blanco para contraste
val lightErrorContainer = Color(0xFFFFDAD6) // Rojo muy claro
val lightOnErrorContainer = Color(0xFF410002) // Rojo muy oscuro

// Colores de Fondo y Superficie
val lightBackground = Color(0xFFFAFDFC) // Blanco ligeramente verdoso
val lightOnBackground = Color(0xFF191D1B) // Gris verdoso muy oscuro para texto

val lightSurface = Color(0xFFFAFDFC) // Igual que background
val lightOnSurface = Color(0xFF191D1B) // Gris verdoso muy oscuro

val lightSurfaceVariant = Color(0xFFDBE5E1) // Gris verdoso claro
val lightOnSurfaceVariant = Color(0xFF3F4A46) // Gris verdoso medio

// Colores Especiales
val lightSurfaceTint = lightPrimary // Tinte basado en primario
val lightInverseSurface = Color(0xFF2E322F) // Gris verdoso oscuro para elementos invertidos
val lightInverseOnSurface = Color(0xFFEFF2EF) // Blanco verdoso
val lightInversePrimary = Color(0xFF4DD0C7) // Verde claro para elementos invertidos

// Colores de Contorno
val lightOutline = Color(0xFF6F7975) // Gris verdoso medio para bordes
val lightOutlineVariant = Color(0xFFBFC9C5) // Gris verdoso claro para bordes suaves

// Colores de Superficie Adicionales
val lightScrim = Color(0xFF000000) // Negro para overlay
val lightSurfaceBright = Color(0xFFFAFDFC) // Superficie brillante
val lightSurfaceContainer = Color(0xFFEEF1EE) // Contenedor neutro
val lightSurfaceContainerHigh = Color(0xFFE8EBE8) // Contenedor elevado
val lightSurfaceContainerHighest = Color(0xFFE2E5E2) // Contenedor más elevado
val lightSurfaceContainerLow = Color(0xFFF4F7F4) // Contenedor bajo
val lightSurfaceContainerLowest = Color(0xFFFFFFFF) // Contenedor más bajo
val lightSurfaceDim = Color(0xFFDADDDA) // Superficie atenuada

// =================== TEMA OSCURO ===================

// Colores Primarios - Verde más claro para tema oscuro
val darkPrimary = Color(0xFF4DD0C7) // Verde azulado claro brillante
val darkOnPrimary = Color(0xFF003730) // Verde oscuro para contraste
val darkPrimaryContainer = Color(0xFF004F45) // Verde medio
val darkOnPrimaryContainer = Color(0xFFB2DFDB) // Verde muy claro

// Colores Secundarios
val darkSecondary = Color(0xFFB0CCC3) // Verde grisáceo claro
val darkOnSecondary = Color(0xFF1B312A) // Verde grisáceo oscuro
val darkSecondaryContainer = Color(0xFF324840) // Verde grisáceo medio
val darkOnSecondaryContainer = Color(0xFFCCE8DF) // Verde grisáceo muy claro

// Colores Terciarios
val darkTertiary = Color(0xFFC5D3A2) // Verde oliva claro
val darkOnTertiary = Color(0xFF2F3515) // Verde oliva oscuro
val darkTertiaryContainer = Color(0xFF454D2A) // Verde oliva medio
val darkOnTertiaryContainer = Color(0xFFE1EFBE) // Verde oliva muy claro

// Colores de Error (mantener igual)
val darkError = Color(0xFFFFB4AB) // Rojo claro
val darkOnError = Color(0xFF690005) // Rojo oscuro
val darkErrorContainer = Color(0xFF93000A) // Rojo medio
val darkOnErrorContainer = Color(0xFFFFDAD6) // Rojo muy claro

// Colores de Fondo y Superficie
val darkBackground = Color(0xFF0F1713) // Gris verdoso muy oscuro
val darkOnBackground = Color(0xFFE0E3E0) // Gris verdoso muy claro

val darkSurface = Color(0xFF0F1713) // Igual que background
val darkOnSurface = Color(0xFFE0E3E0) // Gris verdoso muy claro

val darkSurfaceVariant = Color(0xFF3F4A46) // Gris verdoso medio
val darkOnSurfaceVariant = Color(0xFFBFC9C5) // Gris verdoso claro

// Colores Especiales
val darkSurfaceTint = darkPrimary // Tinte basado en primario
val darkInverseSurface = Color(0xFFE0E3E0) // Gris verdoso claro para elementos invertidos
val darkInverseOnSurface = Color(0xFF2E322F) // Gris verdoso oscuro
val darkInversePrimary = Color(0xFF00695C) // Verde original para elementos invertidos

// Colores de Contorno
val darkOutline = Color(0xFF89938F) // Gris verdoso medio para bordes
val darkOutlineVariant = Color(0xFF3F4A46) // Gris verdoso para bordes suaves

// Colores de Superficie Adicionales
val darkScrim = Color(0xFF000000) // Negro para overlay
val darkSurfaceBright = Color(0xFF353935) // Superficie brillante
val darkSurfaceContainer = Color(0xFF1B1F1C) // Contenedor neutro
val darkSurfaceContainerHigh = Color(0xFF252A26) // Contenedor elevado
val darkSurfaceContainerHighest = Color(0xFF303530) // Contenedor más elevado
val darkSurfaceContainerLow = Color(0xFF141814) // Contenedor bajo
val darkSurfaceContainerLowest = Color(0xFF0A0F0C) // Contenedor más bajo
val darkSurfaceDim = Color(0xFF0F1713) // Superficie atenuada

// =================== COLORES ESPECIALES ===================

// Colores de estado personalizados
val Success = Color(0xFF2E7D32) // Verde más oscuro para éxito
val Warning = Color(0xFFE65100) // Naranja para advertencias
val Info = Color(0xFF0277BD) // Azul para información

// Colores de estado para tema oscuro
val SuccessDark = Color(0xFF66BB6A) // Verde claro
val WarningDark = Color(0xFFFFB74D) // Naranja claro
val InfoDark = Color(0xFF64B5F6) // Azul claro

// Colores adicionales para la UI
val Divider = Color(0xFFE0E0E0) // Divisores en tema claro
val DividerDark = Color(0xFF333333) // Divisores en tema oscuro

val Shadow = Color(0x1A000000) // Sombra ligera
val ShadowDark = Color(0x33000000) // Sombra más intensa para tema oscuro

// Gradientes sugeridos (para uso opcional)
val GradientPrimary = listOf(
    lightPrimary,
    Color(0xFF00796B)
)

val GradientPrimaryDark = listOf(
    darkPrimary,
    Color(0xFF80CBC4)
)

// Colores para elementos interactivos
val ButtonPrimary = lightPrimary
val ButtonPrimaryDark = darkPrimary
val ButtonSecondary = lightSecondary
val ButtonSecondaryDark = darkSecondary

// Colores para texto
val TextPrimary = lightOnBackground
val TextPrimaryDark = darkOnBackground
val TextSecondary = Color(0xFF5F6F65)
val TextSecondaryDark = Color(0xFFB8C5BB)
val TextDisabled = Color(0xFFA5B5A8)
val TextDisabledDark = Color(0xFF5A6B5E)