package org.override.apollo.ui.theme

import androidx.compose.ui.graphics.Color

// =================== TEMA CLARO ===================

// Colores Primarios - Azul profesional y elegante
val lightPrimary = Color(0xFF1565C0) // Azul profundo profesional
val lightOnPrimary = Color(0xFFFFFFFF) // Blanco para contraste
val lightPrimaryContainer = Color(0xFFD4E3FF) // Azul muy claro para contenedores
val lightOnPrimaryContainer = Color(0xFF001B3D) // Azul muy oscuro para texto

// Colores Secundarios - Azul grisáceo complementario
val lightSecondary = Color(0xFF545F71) // Azul grisáceo para elementos secundarios
val lightOnSecondary = Color(0xFFFFFFFF) // Blanco para contraste
val lightSecondaryContainer = Color(0xFFD8E3F8) // Azul grisáceo claro
val lightOnSecondaryContainer = Color(0xFF111C2B) // Azul grisáceo muy oscuro

// Colores Terciarios - Verde azulado para elementos de acento
val lightTertiary = Color(0xFF6B5D42) // Marrón dorado para elementos especiales
val lightOnTertiary = Color(0xFFFFFFFF) // Blanco para contraste
val lightTertiaryContainer = Color(0xFFF4E1BE) // Dorado muy claro
val lightOnTertiaryContainer = Color(0xFF231B04) // Marrón muy oscuro

// Colores de Error - Rojo profesional
val lightError = Color(0xFFBA1A1A) // Rojo profesional
val lightOnError = Color(0xFFFFFFFF) // Blanco para contraste
val lightErrorContainer = Color(0xFFFFDAD6) // Rojo muy claro
val lightOnErrorContainer = Color(0xFF410002) // Rojo muy oscuro

// Colores de Fondo y Superficie
val lightBackground = Color(0xFFFDFCFF) // Blanco ligeramente azulado
val lightOnBackground = Color(0xFF1A1C1E) // Gris muy oscuro para texto

val lightSurface = Color(0xFFFDFCFF) // Igual que background
val lightOnSurface = Color(0xFF1A1C1E) // Gris muy oscuro

val lightSurfaceVariant = Color(0xFFE0E2EC) // Gris azulado claro
val lightOnSurfaceVariant = Color(0xFF43474E) // Gris azulado medio

// Colores Especiales
val lightSurfaceTint = lightPrimary // Tinte basado en primario
val lightInverseSurface = Color(0xFF2F3033) // Gris oscuro para elementos invertidos
val lightInverseOnSurface = Color(0xFFF1F0F4) // Blanco grisáceo
val lightInversePrimary = Color(0xFFA4C8FF) // Azul claro para elementos invertidos

// Colores de Contorno
val lightOutline = Color(0xFF73777F) // Gris medio para bordes
val lightOutlineVariant = Color(0xFFC3C6CF) // Gris claro para bordes suaves

// Colores de Superficie Adicionales
val lightScrim = Color(0xFF000000) // Negro para overlay
val lightSurfaceBright = Color(0xFFFDFCFF) // Superficie brillante
val lightSurfaceContainer = Color(0xFFF1F0F4) // Contenedor neutro
val lightSurfaceContainerHigh = Color(0xFFEBEAEE) // Contenedor elevado
val lightSurfaceContainerHighest = Color(0xFFE5E4E8) // Contenedor más elevado
val lightSurfaceContainerLow = Color(0xFFF7F6FA) // Contenedor bajo
val lightSurfaceContainerLowest = Color(0xFFFFFFFF) // Contenedor más bajo
val lightSurfaceDim = Color(0xFFDDD9DD) // Superficie atenuada

// =================== TEMA OSCURO ===================

// Colores Primarios - Azul más claro para tema oscuro
val darkPrimary = Color(0xFFA4C8FF) // Azul claro brillante
val darkOnPrimary = Color(0xFF003062) // Azul oscuro para contraste
val darkPrimaryContainer = Color(0xFF004B87) // Azul medio
val darkOnPrimaryContainer = Color(0xFFD4E3FF) // Azul muy claro

// Colores Secundarios
val darkSecondary = Color(0xFFBCC7DC) // Azul grisáceo claro
val darkOnSecondary = Color(0xFF263141) // Azul grisáceo oscuro
val darkSecondaryContainer = Color(0xFF3C4858) // Azul grisáceo medio
val darkOnSecondaryContainer = Color(0xFFD8E3F8) // Azul grisáceo muy claro

// Colores Terciarios
val darkTertiary = Color(0xFFD7C5A2) // Dorado claro
val darkOnTertiary = Color(0xFF392F15) // Marrón oscuro
val darkTertiaryContainer = Color(0xFF51462A) // Marrón medio
val darkOnTertiaryContainer = Color(0xFFF4E1BE) // Dorado muy claro

// Colores de Error
val darkError = Color(0xFFFFB4AB) // Rojo claro
val darkOnError = Color(0xFF690005) // Rojo oscuro
val darkErrorContainer = Color(0xFF93000A) // Rojo medio
val darkOnErrorContainer = Color(0xFFFFDAD6) // Rojo muy claro

// Colores de Fondo y Superficie
val darkBackground = Color(0xFF111318) // Gris azulado muy oscuro
val darkOnBackground = Color(0xFFE3E1E6) // Gris muy claro

val darkSurface = Color(0xFF111318) // Igual que background
val darkOnSurface = Color(0xFFE3E1E6) // Gris muy claro

val darkSurfaceVariant = Color(0xFF43474E) // Gris azulado medio
val darkOnSurfaceVariant = Color(0xFFC3C6CF) // Gris azulado claro

// Colores Especiales
val darkSurfaceTint = darkPrimary // Tinte basado en primario
val darkInverseSurface = Color(0xFFE3E1E6) // Gris claro para elementos invertidos
val darkInverseOnSurface = Color(0xFF2F3033) // Gris oscuro
val darkInversePrimary = Color(0xFF1565C0) // Azul original para elementos invertidos

// Colores de Contorno
val darkOutline = Color(0xFF8D9199) // Gris medio para bordes
val darkOutlineVariant = Color(0xFF43474E) // Gris azulado para bordes suaves

// Colores de Superficie Adicionales
val darkScrim = Color(0xFF000000) // Negro para overlay
val darkSurfaceBright = Color(0xFF37393C) // Superficie brillante
val darkSurfaceContainer = Color(0xFF1E2024) // Contenedor neutro
val darkSurfaceContainerHigh = Color(0xFF292A2E) // Contenedor elevado
val darkSurfaceContainerHighest = Color(0xFF333438) // Contenedor más elevado
val darkSurfaceContainerLow = Color(0xFF17191C) // Contenedor bajo
val darkSurfaceContainerLowest = Color(0xFF0C0E11) // Contenedor más bajo
val darkSurfaceDim = Color(0xFF111318) // Superficie atenuada

// =================== COLORES ESPECIALES ===================

// Colores de estado personalizados
val Success = Color(0xFF4CAF50) // Verde para éxito
val Warning = Color(0xFFFF9800) // Naranja para advertencias
val Info = Color(0xFF2196F3) // Azul para información

// Colores de estado para tema oscuro
val SuccessDark = Color(0xFF81C784) // Verde claro
val WarningDark = Color(0xFFFFB74D) // Naranja claro
val InfoDark = Color(0xFF64B5F6) // Azul claro

// Colores adicionales para la UI
val Divider = Color(0xFFE0E0E0) // Divisores en tema claro
val DividerDark = Color(0xFF333333) // Divisores en tema oscuro

val Shadow = Color(0x1A000000) // Sombra ligera
val ShadowDark = Color(0x33000000) // Sombra más intensa para tema oscuro

// Gradientes sugeridos (para uso opcional)
val GradientPrimary = listOf(
    lightPrimary,
    Color(0xFF1976D2)
)

val GradientPrimaryDark = listOf(
    darkPrimary,
    Color(0xFF90CAF9)
)

// Colores para elementos interactivos
val ButtonPrimary = lightPrimary
val ButtonPrimaryDark = darkPrimary
val ButtonSecondary = lightSecondary
val ButtonSecondaryDark = darkSecondary

// Colores para texto
val TextPrimary = lightOnBackground
val TextPrimaryDark = darkOnBackground
val TextSecondary = Color(0xFF757575)
val TextSecondaryDark = Color(0xFFBDBDBD)
val TextDisabled = Color(0xFFBDBDBD)
val TextDisabledDark = Color(0xFF616161)